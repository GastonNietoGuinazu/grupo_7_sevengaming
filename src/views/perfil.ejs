<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://kit.fontawesome.com/6d7ddf6bb9.js" crossorigin="anonymous"></script>
  <link rel="stylesheet" href="/css/home.css">
  <link rel="stylesheet" href="/css/producto.css" />
  <link rel="stylesheet" href="/css/listaProductos.css" />
  <title><%= user.first_name %> <%= user.last_name %></title>
</head>

<body>
  <!-- Header -->
  <%- include('partials/header') %>
  <main>
    <div class="container-prueba">
      <h1 class="listado-productos"><%= user.first_name %> <%= user.last_name %></h1>
      <div class="cards-container">
        <ul class="items-container">
          <img class="img-game" src="/images/csgopersonaje2.jpeg" alt="foto perfil" />
          <li class="items">
              <p>Nombre: <%= user.first_name %></p>
              <p>Apellido: <%= user.last_name %></p>
              <p>Email: <%= user.email %></p>
              <p>Categoría: <% if (user.categoryId == 1){%> Administrador
              <%} else {%> Cliente <%}%></p>
          </li>
        </ul>
      </div>
      <div class="cards-container">
        <a href="/usuarios/editar/<%= user.id %>"><button class="botom--format" type="button">Editar Perfil</button></a>
        <a href="/usuarios/eliminar/<%= user.id %>"><button class="botom--format" type="button">Eliminar Cuenta</button></a>
      </div>
    </div>    
  </main>
  <!-- Footer -->
  <%- include('partials/footer') %>
</body>

</html>